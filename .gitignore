.pio
.vscode/.browse.c_cpp.db*
.vscode/ipch
.vscode/vsix
compile_commands.json

# LED Show Studio
led-studio/node_modules/
led-studio/out/
led-studio/dist/
led-studio/release/

# macOS
.DS_Store

# 產生物：waveform 快取（由 electron/services/waveformCache.ts 產生）
.led-cache/

# 本地測試素材，不入庫（檔案仍留在工作目錄）
music.mp3
*.mp3

# 本機環境變數（含 Wi-Fi 憑證等），一律不入庫
.env
.env.*
!.env.example

# PlatformIO IDE 自動產生的檔案，內含本機絕對路徑（家目錄、toolchain 位置），
# 換機器就失效；兩者開頭都標明 AUTOMATICALLY GENERATED，pio 會自行重新產生。
# extensions.json / settings.json / tasks.json 不含絕對路徑，保留在版控中。
.vscode/c_cpp_properties.json
.vscode/launch.json

# Claude Code 的本機工作目錄（含 worktree 副本，可達 GB 級），不入庫
.claude/
